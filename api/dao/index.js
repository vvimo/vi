const articleDao = require('./article')
const cardDao = require('./card')
const loginDao = require('./login')
const userDao = require('./user')

module.exports = {
  articleDao,
  cardDao,
  loginDao,
  userDao
}
